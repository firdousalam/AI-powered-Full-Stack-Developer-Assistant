/**
 * Git MCP Server.
 *
 * MCP server initialization and Gateway integration will be
 * implemented in subsequent milestones.
 */

import {
    DEFAULT_GIT_DIFF_FILE_LIMIT,
    MAX_GIT_DIFF_FILE_LIMIT,
    DEFAULT_GIT_DIFF_SIZE_LIMIT,
    MAX_GIT_DIFF_SIZE_LIMIT,
} from './git.constants';

import type {
    GitDiff,
    GitDiffFile,
    GitDiffOptions,
} from './git.types';

export class GitMcpServer {


}